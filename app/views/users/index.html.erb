<h1>User index</h1>

<%# debugging %= %>
<%# debug @users %>
<%# @users.inspect %>
<%# simple_format @users.to_yaml %>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, edit_user_path(user) %> 
    </li>
  <% end %>
</ul>

<%= link_to "Register User", new_user_path %>
<%= link_to "Sign out", signout_path %>